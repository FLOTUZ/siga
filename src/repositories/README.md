# Repositories

Un `Repository` representa una interfaz `Service` especializada que proporciona operaciones de acceso a datos de tipo fuerte (por ejemplo, CRUD) de un modelo de dominio contra la base de datos o el servicio subyacente.

Este directorio contiene código para repositorios proporcionados por esta aplicación.

`lb4 repository`
